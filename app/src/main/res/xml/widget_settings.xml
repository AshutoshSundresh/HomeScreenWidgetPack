<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <PreferenceCategory
        android:title="Music Widget">

        <SwitchPreferenceCompat
            android:key="blur_effect"
            android:title="Enable blur effect"
            android:summary="Apply a blur effect to the album art background"
            android:defaultValue="true" />

        <SeekBarPreference
            android:key="blur_strength"
            android:title="Blur strength"
            android:summary="Controls the intensity of the blur effect"
            android:defaultValue="10"
            android:max="25"
            android:dependency="blur_effect"/>

        <SwitchPreferenceCompat
            android:key="gradient_overlay"
            android:title="Enable gradient overlay"
            android:summary="Adds a top-to-bottom gradient over the album art"
            android:defaultValue="true" />

        <SeekBarPreference
            android:key="gradient_darkness"
            android:title="Gradient darkness"
            android:summary="Controls the darkness of the gradient overlay"
            android:defaultValue="30"
            android:max="100"
            android:dependency="gradient_overlay"/>

        <SeekBarPreference
            android:key="gradient_opacity"
            android:title="Gradient opacity"
            android:summary="Controls the opacity of the gradient overlay"
            android:defaultValue="80"
            android:max="100"
            android:dependency="gradient_overlay"/>

        <SwitchPreferenceCompat
            android:key="general_darkness_overlay"
            android:title="Enable general darkness"
            android:summary="Apply a dark overlay to the entire album art"
            android:defaultValue="false" />

        <SeekBarPreference
            android:key="general_darkness_strength"
            android:title="General darkness strength"
            android:summary="Controls the strength of the dark overlay"
            android:defaultValue="40"
            android:max="100"
            android:dependency="general_darkness_overlay"/>

        <SwitchPreferenceCompat
            android:key="use_app_icon"
            android:title="Use app icon"
            android:summary="Use the app's icon instead of the color-tinted notification icon"
            android:defaultValue="false" />

    </PreferenceCategory>

</PreferenceScreen>