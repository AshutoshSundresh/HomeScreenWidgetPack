<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark theme with dynamic colors. -->
    <style name="Base.Theme.HomeScreenWidgetPack" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Dynamic colors are automatically applied -->
        
        <!-- Status bar and navigation bar styling for dark mode -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>
    </style>
</resources>