<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme with dynamic colors. -->
    <style name="Base.Theme.HomeScreenWidgetPack" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Enable dynamic colors on Android 12+ -->
        <!-- These will automatically adapt to user's wallpaper colors -->
        
        <!-- Status bar and navigation bar styling -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
    </style>

    <style name="Theme.HomeScreenWidgetPack" parent="Base.Theme.HomeScreenWidgetPack" />
</resources>